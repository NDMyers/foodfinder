# Google Maps API Key (client-side - for map rendering)
# This key will be exposed to browsers, so restrict it to your domain
NEXT_PUBLIC_MAPS_API_KEY=your_maps_api_key_here

# Alternative server-only key (optional - if you want separate keys)
# MAPS_API=your_server_only_key_here
# GOOGLE_PLACES_SERVER_KEY=your_server_only_key_here
